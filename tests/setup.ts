// tests/setup.ts
import { beforeAll, afterAll } from 'vitest';

// Add any test global setups here
beforeAll(() => {
  // Setup mock servers, db connections, etc.
});

afterAll(() => {
  // Cleanup after tests
});
